# git config --add include.path ../.github/template/gitconfig
# git config --add include.path ../.github/template/gitconfig

[alias]
co = checkout
st = status
root = rev-parse --show-toplevel

[push]
    default = current